package de.mycraftnote.code_channlenge.domain.factory

import de.mycraftnote.code_channlenge.domain.model.ProjectsModel

internal object Factory {
  val model1 = ProjectsModel(
    projects = listOf()
  )
}